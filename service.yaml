---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginxhello
  name: nginxhello
  annotations:
    fluxcd.io/ignore: "false"
spec:
  type: NodePort
  ports:
  - port: 31317
    protocol: TCP
    targetPort: 80
  selector:
    app: nginxhello
